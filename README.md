# Frontend

## Install the dependencies and start the app
```bash
npm install
quasar dev
```

# Backend

## Install the dependencies and start the app
```bash
pip install -r requirements.txt
flask run --port=5004
```

